interface:
  display_name: "Task Runner"
  short_description: "Execute and independently verify a task plan"
  default_prompt: "Use $task-runner to execute and verify the attached implementation plan."
