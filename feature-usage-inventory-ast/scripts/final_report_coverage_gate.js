#!/usr/bin/env node
"use strict";const fs=require("node:fs"),path=require("node:path");const {normalizeCapabilities}=require("./capability_contract");try{const a=process.argv.slice(2),get=f=>a[a.indexOf(f)+1],facts=JSON.parse(fs.readFileSync(path.resolve(get("--facts")))),report=fs.readFileSync(path.resolve(get("--report")),"utf8"),missing=normalizeCapabilities(facts.capabilities||[]).filter(x=>x.requiredForFinalReport&&!report.includes(x.id));console.log(JSON.stringify({gate:"final-report-coverage",status:missing.length?"missing":"covered",missing:missing.map(x=>x.id)}));if(missing.length)process.exitCode=1;}catch(e){console.log(JSON.stringify({status:"error",errors:[{message:e.message}]}));process.exitCode=2;}
