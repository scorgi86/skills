interface:
  display_name: "Feature Usage Inventory AST"
  short_description: "Compact staged Graph/AST inventory with canonical JSON artifacts"
  default_prompt: "Use $feature-usage-inventory-ast with continuous goal-driven execution by default, persist compact canonical JSON stage artifacts, preserve typed planning facts, and verify GitNexus/AST candidates in source."
