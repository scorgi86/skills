# Направления оптимизации task-research

Зафиксировано: 2026-09-06 по итогам обсуждения с пользователем.

Статус: часть направлений реализована после фиксации списка; остальные остаются рекомендациями, а не командой на реализацию. Перед началом конкретной доработки сверять исходное состояние с актуальным кодом. Численные оценки ускорения не подтверждены замерами.

## Приоритеты и рекомендации

P1 — ближайшие изменения; P2 — следующий этап; P3 — по результатам замеров или при появлении потребности.

| Приоритет | Направление | Рекомендация | Польза и условия |
|---|---|---|---|
| Выполнено | Измерение Stage 1–7 | Добавлен benchmark холодного Stage 1 и повторного Stage 2 с разложением AST/evidence/residual, проверкой provenance и семантической эквивалентности | Исторический промежуточный прогон показал 29,28%, но его baseline runtime не сохранён. Новый воспроизводимый A/B крайних состояний фиксируется отдельно и не смешивается с ним |
| Выполнено | Исправление кеша | Кеш проверяется до парсинга; исходник читается один раз, байты используются для хеша и анализа. Добавлены версии, проверка записи и безопасная публикация | HIT пропускает SWC и извлечение; кеш остаётся opt-in. Контракт: [ast-workflow.md](ast-workflow.md#file-analysis-cache) |
| Выполнено | Постоянный файловый кеш Stage 1–2 | Pipeline автоматически назначает общий scope-зависимый cache Stage 1 и Stage 2; явная настройка имеет приоритет, прямой runner остаётся opt-in | Повторный Stage 2 переиспользует анализ между процессами; изменение исходника и повреждение записи приводят к miss и пересчёту |
| Выполнено | Финализация Stage 2 | Coverage gate и budget finalization выполняются по одному разу; direct preparation — один раз, deferred — ни одного. Benchmark показывает отдельные интервалы этих операций | На 5 samples coverage снизился с 18,19 до 10,72 мс, budget — с 63,33 до 34,35 мс. Общий wall не используется как доказательство ускорения из-за одновременного роста времени AST и canonical preparation |
| Выполнено | JSON как основа отчёта | Stage 7 формирует проверяемую report-model; Stage 8 принимает только модель с trusted digest. Полнота, ссылки, статусы и coverage проверяются до рендеринга | Факты не зависят от Markdown-оформления |
| Выполнено | Проверка генерации Markdown | Stage 8 тестирует и строго валидирует детерминированные проекции: обязательные поля, ссылки, экранирование, candidate-статусы, provenance и product gaps | Digest связывает модель с результатом, а bundle-валидация проверяет смысл проекции |
| Выполнено | Разделение крупных модулей | Runtime разнесён по CLI, flow, state, shared AST/evidence/report/search и stage-модулям; тесты следуют за логическими блоками | Публичные entry points сохранены; дальнейшее дробление — только при новой самостоятельной ответственности |
| Выполнено | Единый контекст запуска | Pipeline владеет одной `InventorySession`, передаёт runner’ам immutable StageContext с transition и валидированной lineage; init и revision используют общий StateStore | Убраны повторные чтения transition внутри Stage 1–6 и повторная lineage-валидация Stage 7; прямые API сохраняют файловый fallback |
| P1 | Точечный режим quick | Добавить удобную команду поверх targeted workflow: поиск, проверка совпадений, краткий результат | Упростить точечные вопросы; сокращённый проход не должен означать завершённую полную инвентаризацию |
| P2 | Параллельный AST-анализ | Ограниченный настраиваемый пул workers; небольшие пакеты выполнять последовательно; возвращать символы и связи | Ускорить большие холодные запуски; учитывать память и сохранять детерминированный порядок результатов |
| Выполнено | Параллельный Stage 0 | Репозитории сканируются асинхронным `rg` через ограниченный пул; `request.searchConcurrency` задаёт предел | На `sdkjs`, `web-apps`, `desktop-apps` измерено 6,97% ускорения (`1529` → `1423` мс), поэтому default оставлен `1`, параллельность включается явно |
| P2 | Инкрементальный пересчёт фактов | Ввести снимок области поиска и зависимости фактов от файлов, запросов и других фактов | Учитывать незакоммиченные изменения, добавления, удаления и переименования; одного lastProcessedCommit недостаточно |
| P2 | Инвалидирование выводов об отсутствии | Связывать отрицательные результаты со всей проверенной областью и её составом | Новый файл должен отменять устаревший вывод «использований нет», даже если прежние файлы неизменны |
| P2 | Чтение evidence | Использовать существующие selectors; читать и валидировать артефакт один раз на пакет запросов. Затем оценить индексированное хранение | Снизить повторные чтения и расход памяти; ограничение выдачи не предотвращает полную загрузку JSON |
| P2 | Пагинация | Добавить продолжение выборки через cursor или offset, сохраняя totals и признак усечения | Последовательно получать все результаты; limit уже существует |
| P2 | Бюджеты | Разделить ограничения полного набора фактов и представления; учитывать локальные лимиты запросов | Управлять объёмом без скрытой потери полноты; усечение обязательных данных сохраняет partial |
| P2 | Обработка ошибок | Унифицировать машинные коды, контекст, формат CLI и exit codes на внешних границах | Упростить диагностику и автоматизацию; внутренние исключения и результаты не обязательно делать одинаковыми |
| P2 | Очистка артефактов | Отдельная команда с предварительным списком удаления, политикой хранения и проверкой зависимостей между запусками | Контролировать диск, сохраняя используемые evidence и историю происхождения |
| P3 | Сжатие артефактов | После замеров рассмотреть gzip/brotli. fileId уже реализован; MessagePack пока отложить | Экономия диска с учётом распаковки, целостности и совместимости форматов |
| P3 | Индексация исходников | Вводить при подтверждённом узком месте; индекс использовать для поиска кандидатов с последующей проверкой исходника | mtime недостаточно для доказательности; токенный индекс не заменяет regex-поиск и не гарантирует O(1) для всего запроса |
| P3 | Пользовательские шаблоны | Добавлять при потребности в вариантах оформления; сохранять обязательное содержание и проверки рендерера | Гибкость отчётов; небольшой Stage 8 не требует срочной декомпозиции |
| P3 | Режим ast-only | Диагностическая выдача кандидатов без подтверждённых выводов и продвижения canonical state | Быстрая предварительная оценка с ограниченной доказательностью |

## Архитектурные решения

### JSON-модель и отчёты

Целевая последовательность: факты → JSON-модель → проверка структуры и смысла → Markdown.

JSON Schema проверяет обязательные поля, типы и допустимые статусы. Смысловой валидатор проверяет разрешимость evidence-ссылок, полноту покрытия, основания для отсутствия использований и согласованность выводов. Одной JSON Schema недостаточно.

Реализовано: Stage 7 формирует report-model, Stage 8 связан с её digest, а модель и bundle-проекция проходят отдельные проверки. Markdown не используется как переходный вход и не является источником фактов. Разбор Markdown сохранять только при появлении отдельного сценария импорта старых или вручную отредактированных отчётов.

### Два уровня инкрементальности

1. Переиспользование файлового анализа: неизменённый файл не парсится повторно, запросы и итоговые проверки выполняются заново.
2. Переиспользование фактов: пересчитываются зависимые выводы; необходимы снимок области поиска и явные правила инвалидирования.

Сначала реализовать первый уровень. Для второго учитывать изменения scope, запросов, настроек и версий анализа наряду с изменениями файлов. Закрытые артефакты сохранять неизменными; новый запуск получает новую ревизию с происхождением переиспользованных данных. Кешированные AST-кандидаты сами по себе не становятся source-confirmed evidence.

### Разделение файлов

Реализовано: файловый анализ, кеш, evidence, модель отчёта, renderer, state и pipeline разделены по самостоятельным обязанностям. `stage_pipeline.js` и `stage8_runner.js` не дробились ради размера.

Сохранять CLI и экспорты через прежние точки входа; передавать зависимости явно, избегать циклических импортов и общего `utils.js` без конкретного назначения. Новое извлечение модулей делать только при появлении самостоятельной причины изменения.

## Последовательность и критерии приёмки

Выполнены исправление кеша, перенос проверки фактов на JSON-модель, проверка renderer, связанная декомпозиция и профилирование Stage 1–7. Pipeline переиспользует AST между Stage 1 и Stage 2 и выполняет канонизацию Stage 2 один раз. Исторический промежуточный прогон на внутренних тенях показал снижение Stage 1+2 с 16 024 до 11 333 мс (29,28%), residual Stage 2 — с 9 330 до 3 476 мс (62,75%). Его временная baseline-реализация и `implementationDigest` не были сохранены, поэтому результат не воспроизводится как строгий A/B и не характеризует всю последующую серию изменений.

Воспроизводимое сравнение крайних состояний `16ced79` и `ec3e8a6` на одинаковом текущем workload внутренних теней показало рост Stage 1+2 wall time с 10 836,83 до 12 137,02 мс (12,00%). AST и evidence Stage 2 ускорились на 3,43% и 6,93%, но residual вырос из-за канонизации и новых доказательных проверок. Stage 1 evidence и ownership совпали, а AST исправил два слабых кандидата `target: unknown`; поэтому это изменение цены и качества контракта, а не чистый performance A/B. Методология и данные: [benchmark-methodology.md](benchmark-methodology.md) и [internal-shadows-all-optimizations-ab-20260909.md](benchmarks/internal-shadows-all-optimizations-ab-20260909.md).

Benchmark запускается командой `npm run benchmark:stages -- --runtime <skill-root> --requests <request-pack.json> --output <result.json> --warmup 1 --runs 3`. Для сравнения добавляется `--compare <baseline.json>`. Сравнение отклоняет несовпадающие harness, requests, environment, target repositories и семантические результаты.

Принимать оптимизации по сохранению содержательных результатов, evidence-ссылок и статусов полноты при сравнении полного и оптимизированного запусков. Проверять неизменённый повторный запуск, изменение файла, добавление использования в новом файле, удаление, переименование, незакоммиченные изменения и изменение настроек анализа. При ошибках парсинга или усечении сохранять неизвестность и ограничения, не превращать их в доказательство отсутствия.

Для декомпозиции проверять сохранение внешних интерфейсов и поведения. Для JSON-валидации — перенос существующих смысловых правил и сохранение обязательной информации при рендеринге. Зафиксированные рекомендации не вводят новые требования в текущий runtime до отдельной реализации.
