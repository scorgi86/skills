# Состояние task-research на 2026-09-18

Это снимок текущей рабочей копии, а не новый контракт навыка и не план реализации. База Git: `7fdfa3864ba2fb04d1ffb0dc3afddd6cd4de738a`. В `task-research` на момент снимка уже были незакоммиченные изменения; этот документ не утверждает, что они приняты или проверены единым прогоном.

## Действующий контракт

- Runtime навыка: `4.1.0`; схема канонических артефактов и состояния: `4.0.0`.
- Полное исследование проходит Stage 0–8 в режиме `continuous`. Основной маршрут — `full_run`; `stage_pipeline` выполняет и проверяет отдельный этап. Продвижение состояния разрешено только после закрытия и сохранения канонического результата.
- Передача между этапами опирается на `canonical/stage-result.json` и отдельный `canonical/evidence.json`. Markdown не является входом следующего этапа. GitNexus и AST дают кандидатов; подтверждение требует текущего исходника и точной привязки.
- Stage 0 фиксирует scope, seeds, профиль полноты и решение о Stage 6. Его канонический summary содержит количество найденных файлов по репозиториям, но не полный список этих файлов как готовый вход Stage 1.
- Stage 1 принимает заранее составленные AST/source/GitNexus-запросы, группы владения, подтверждения и coverage contract. Stage 2 также принимает AST/source-запросы. Stage 3 читает boundary-факты, но получает consumer scopes из запроса. Stage 4 требует recipient families, Stage 5 — targeted checks и name coverage. Stage 6 выполняет выбранное сравнение либо закрытый skip. Stage 7 собирает и валидирует report model, Stage 8 проверяет digest и рендерит без исследования.

## Наблюдение в новом прогоне

Чистый по входным артефактам прогон запроса «Найди все о внутренних тенях» по `sdkjs` и `web-apps` сохранён в `C:/work/skills/inventory-artifacts/internal-shadows-clean-20260918-1354`. Старые исследовательские результаты и заготовки запросов не использовались; знание из истории чата исключить нельзя.

- Stage 0: закрыт; 22 файла-кандидата в `sdkjs`, 11 в `web-apps`; запуск скрипта — 29,430 с.
- Stage 1: закрыт после девяти запусков скрипта (суммарно 38,113 с). Между завершением Stage 0 и закрытием Stage 1 прошло 626,873 с; остаток около 588,760 с включает ручную подготовку, чтение, диагностику, правки запроса и накладные расходы инструментов, а не только вычисления ИИ.
- Stage 2: закрыт; запуск скрипта — 0,726 с; новых строк в `facts` не добавил.
- Перед Stage 3 прогон остановлен: в закрытой lineage нет boundary-факта `sdkjs → web-apps`. Текущий Stage 3 строит поиск потребителя из таких фактов; продолжение с пустым набором не подтвердило бы полноту. Stage 3–8 этим прогоном не измерены.
- Из-за ручного запроса Stage 1 факт `CEffectLst.innerShdw` был помечен `confirmed` по строке `this.innerShdw = null`. Привязка к файлу подлинная, но эта строка не доказывает тип хранимого значения; подтверждающая строка `this.innerShdw = new CInnerShadow()` находится в `sdkjs/common/Drawings/Format/Format.js:7588`. Это обнаруженный смысловой дефект результата прогона, не доказательство правильной связи.

Точные попытки и оговорки по времени: `C:/work/skills/inventory-artifacts/internal-shadows-clean-20260918-1354/measurements.json`.

## Граница текущей автоматизации

Скрипты уже собирают кандидатов, проверяют форму и свежесть доказательств, канонизируют факты и управляют транзакционным переходом. Между этапами ИИ пока составляет существенную часть семантических запросов и интерпретирует результаты. Проверка source anchor удостоверяет строку, но сама по себе не доказывает смысл `relation`.

Обсуждённая архитектура «конвейер доказательств с ИИ по исключениям» **не реализована**. Для неё обсуждались типизированные связи (`from`, `relation`, `to`), матрица условий доказательства и автомат состояний `candidate / confirmed / unknown / checked-no-usage`. Это направление, а не текущее свойство навыка. Оно требует отдельной проверки полноты: неизвестные и неоднозначные случаи нельзя молча закрывать.

## Идентификаторы снимка

- `task-research/SKILL.md` SHA-256: `cf8b4ec99e7d659b7a27c4ffa83b3d0e854cea502609fdb5e1df3247ce7bbeb3`
- `task-research/scripts/flows/full-flow/src/stage_pipeline.js` SHA-256: `01f1baac635b7548aade5fd5a5806c563c2263baf6f23c4f187ba0211e5cca07`
- Состояние рабочего дерева, а не Git-коммит: ранее существовавшие изменения навыка сохранены без правок и без staging.
