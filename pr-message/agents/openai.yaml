interface:
  display_name: "PR Message"
  short_description: "Create, validate, and persist a reviewer-oriented PR message"
  default_prompt: "Use $pr-message to analyze the relevant Git scope, produce a validated PR description, and save its artifacts in the project task context."
